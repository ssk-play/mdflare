{
  "name": "mdflare",
  "version": "1.0.15",
  "private": true,
  "description": "🔥 MDFlare — Local markdown folder sync to web",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:landing": "npm -w @mdflare/landing run dev",
    "dev:cloud": "npm -w @mdflare/cloud run dev",
    "dev:private": "npm -w @mdflare/private run dev",
    "dev:server": "node server/index.js",
    "build:all": "npm -w @mdflare/landing run build && npm -w @mdflare/cloud run build && npm -w @mdflare/private run build",
    "build:landing": "npm -w @mdflare/landing run build",
    "build:cloud": "npm -w @mdflare/cloud run build",
    "build:private": "npm -w @mdflare/private run build",
    "test:api": "./scripts/check-api-parity.sh"
  },
  "dependencies": {
    "concurrently": "^9.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "wrangler": "^4.62.0"
  },
  "license": "AGPL-3.0"
}
